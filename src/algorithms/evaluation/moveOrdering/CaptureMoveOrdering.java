package algorithms.evaluation.moveOrdering;

import game.Move;


// Moves capture moves to the front of the list
public class CaptureMoveOrdering implements IMoveOrdering {

    @Override
    public int compare(Move o, Move t1) {
        return 0;
    }
}
